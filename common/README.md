# Common Utilities

This directory contains common utilities, headers, and helper functions that can be used across multiple assignments.

## Contents

- `common.h` - Common header file with useful definitions and macros
- Additional utility functions as needed

## Usage

Include the common header in your assignment code:

```c
#include "../common/common.h"
```

Compile with appropriate include paths if needed.
